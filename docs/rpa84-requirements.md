# RPA84 官網功能自動檢查需求對照

## 整合方式

RPA84 需求已依現有架構整合：

- 主程式仍是 `taiwanlife_monitor/monitor.py`。
- 需求與參數直接放在 `config/taiwanlife.json` 的 `rpa84.scenarios`。
- 預設 `rpa84.enabled=false`，避免尚未補齊可執行步驟的流程在正式排程誤報。
- 開啟方式為 CLI `--enable-rpa84` 或環境變數 `MONITOR_ENABLE_RPA84=true`。
- 新增點擊、輸入、驗證規則請看 `docs/rpa84-step-authoring.md`。

## 需求清單

| 步驟 | 名稱 | 狀態 | 備註 |
|---|---|---|---|
| 1-1 | 搜尋全站 | 已納入並啟用 | 預設輸入 `醫療`，驗證搜尋結果文字與截圖 |
| 1-2 | 產品快搜 | 已納入待補步驟 | 補商品快搜的點擊、輸入、結果驗證 |
| 2-1 | 熱門活動搜尋 | 已納入待補步驟 | 補更多活動入口、搜尋、結果驗證 |
| 3-1 | 商品搜尋 | 已納入待補步驟 | 補壽險保障路由與商品搜尋流程 |
| 3-2 | 商品篩選 | 已納入待補步驟 | 補篩選面板、定期壽險、身故金或喪葬費用流程 |
| 3-3 | 商品比較 | 已納入待補步驟 | 補加入比較、比較頁、取消比較驗證 |
| 3-4 | 播放語音簡介 | 已納入待補步驟 | 補播放視窗、暫停、關閉驗證 |
| 3-5 | 旅平險保費試算 | 已納入待補步驟 | 補試算欄位輸入與結果驗證 |
| 4-1 | 理賠試算 | 已納入待補步驟 | 補兩段試算流程與結果驗證 |
| 4-2 | 特約醫院查詢 | 已納入待補步驟 | 補特約體檢與癌症篩檢查詢流程 |
| 5-1 | 房屋貸款試算 | 已納入待補步驟 | 補月付金試算輸入與結果驗證 |
| 5-2 | 宣告利率查詢 | 已納入待補步驟 | 補產品、年份、月份查詢流程 |
| 5-3 | 保險金年金預定利率查詢 | 已納入待補步驟 | 補查詢條件與結果驗證 |
| 5-4 | 分紅保單紅利儲存生息利率查詢 | 已納入待補步驟 | 補產品搜尋與第一列結果驗證 |
| 5-5 | 投資標的查詢 | 已納入待補步驟 | 補標的查詢與欄位皆有值驗證 |
| 5-6 | 房貸指標利率查詢 | 已納入待補步驟 | 補三個利率頁籤與年月查詢流程 |
| 5-7 | 投資標的列印/分享/匯出 | 已納入待補步驟 | 有寄信與下載副作用，正式開啟前需確認測試收件人 |
| 5-8 | 收藏/查看投資標的 | 已納入待補步驟 | 有收藏狀態副作用，需確認清理/反選流程 |
| 5-9 | 投資組合試算 | 已納入待補步驟 | 補比例輸入與圓餅圖驗證 |
| 5-10 | 查詢財經資訊 | 已納入待補步驟 | 補日期與分類查詢流程 |

## 啟用策略

1. 先以目前啟用的 `1-1-search-site` 驗證 RPA84 runner 與報表格式。
2. 每次只打開 1 到 3 個新場景，跑過至少一次 headful 或截圖驗證。
3. 有副作用的場景先在測試收件人、測試帳號或可回復狀態下執行。
4. 場景穩定後，再交給 Windows Task Scheduler、Power Automate 或 n8n 觸發。

## 報表呈現

每個 RPA84 場景會以 `RPA84：<名稱>` 寫入一般巡檢明細，並在 evidence 內保留：

- `scenario_id`
- `group`
- `input`
- `acceptance`
- `steps`
- `screenshot`
- `side_effect`

因此既有 `reports/latest.json`、`reports/latest.md`、stdout `problem_checks` 都能沿用。
